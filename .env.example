# Server-side email settings used only by /api/send-email.
# Do not prefix these variables with VITE_.
# EMAIL_PASS must be a Gmail App Password, not your normal Gmail password.
EMAIL_USER=
EMAIL_PASS=
EMAIL_TO=
